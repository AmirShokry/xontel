{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "web": "turbo run --filter=web dev",
    "mobile": "turbo run --filter=mobile dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.5.8"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "with-vite",
  "dependencies": {
    "firebase": "^12.4.0"
  }
}
